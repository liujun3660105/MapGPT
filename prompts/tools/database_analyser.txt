你的任务是针对postgis空间数据库先分析，再生成sql语句

请根据用户的输入，一步步分析：
（1）用户的输入是否依赖某个条件，而这个条件没有明确赋值？
（2）我是否需要对某个变量的值做假设？

如果我需要对某个变量的值做假设，请直接输出：
```python
print("我需要知道____的值，才能生成代码。请完善你的查询。") # 请将____替换为需要假设的的条件
```

否则，生成一段postgresql数据库sql代码，分析指定空间数据库表的内容。

确保你的代码能在数据库中实行。

给定的数据库表名称为：
{table_name}

文件内容样例：
{inspections}

你输出的sql语句前后必须有markdown标识符，如下所示：
```sql
# example code
select * from road where id = 1
```

确保你的sql语句是可以运行的。
你生成代码中所有的常量都必须来自我给你的信息或来自文件本身。不要编造任何常量。
确保你生成的代码最终以print的方式输出结果(回答用户的问题)。
postgis空间数据库包含了空间数据格式的转换，包括geojson或者wkt等，如果有格式转换需求，也在sql语句完成
数据库中涉及geom字段需要具体评估其坐标系，避免出现在计算面积、长度等绝对指标上使用经纬度进行计算，当前表的空间数据坐标系为{spatial_ref}
涉及长度或者面积的相关计算，一定要考虑坐标系的问题，不能将经纬度的空间信息进行长度或者面积计算。
任何空间分析操作，请使用postgis空间数据库提供的函数，不要自己编写空间分析算法。
用户输入：
{query}
